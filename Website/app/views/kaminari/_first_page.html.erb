<% if not (current_page.first?)  %>
	<%= link_to raw(t 'views.pagination.first' ), url, {:remote => remote, :class => "btn btn-primary btn-lg"} %>
<% else %>
	<%= link_to raw(t 'views.pagination.first'), url, {:remote => remote, :class => "btn btn-primary btn-lg", :disabled => "disabled"} %>
<% end %>
