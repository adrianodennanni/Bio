<% if not (current_page.last?)  %>
	<%= link_to raw(t 'views.pagination.last'), url, {:remote => remote, :class => "btn btn-primary btn-lg"} %>
<% else %>
	<%= link_to raw(t 'views.pagination.last'), url, {:remote => remote, :class => "btn btn-primary btn-lg", :disabled => "disabled"} %>
<% end %>
